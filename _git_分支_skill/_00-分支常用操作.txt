
说明：在git命令行中，拷贝只能用鼠标，不能用ctrl + c


   
--------------------------------------------- 本地分支操作 ------------------------------------------------
 
1. 创建分支
　　git branch dev              //创建dev分支  （应从当前分支master创建出一个一模一样的另一个分支dev）

1. 切换到分支
　　git checkout dev           //切换到dev分支

1. 创建并切换到分支
　　git checkout -b dev      //创建dev分支，并切换到dev分支  （相当于把上面2句合并）    

2. 查看本地分支
      git branch                   //查看本地分支，会列出所有的分支，当前分支前面会添加一个星号

2. 直接看命令行的最后面括号里的分支  
      GS@Lenovo-PC MINGW64 /e/GIT-REPO/TEST04 (develop)   //括号里的分支 develop是当前分支

2. 查看远程分支
     git branch -r                                                                             
2. 本地和远程分支
     git branch -a         

3. 合并分支   
     git merge dev                  //快進合并，因dev分支是从master分支上创建的，把master分支直接快进到dev分支的最新修改。
                                              在master分支里看不到dev分支的历史切换记录
     git merge --no-ff dev      //把dev分支合并到当前分支master上， 在master分支里保留dev的历史切换记录
 
4. 重命名分支
   git branch -m  develop  dev2     //把develop 改名为dev2

5. 删除本地分支
   git branch -d dev       //删除本地的dev分支  //d选项只能删除已经参与了合并的分支
   git branch -D dev       //删除本地的dev分支

  



20. 关于游离分支
     。用git branch -a查看分支时，出现下面的分支信息
       * (HEAD detached from 4774b6a69)

     。当使用 git checkout < branch_name> 切换分支时，HEAD 会移动到指定分支
     。如果使用的是 git checkout < commit id>，即切换到指定的某一次提交，HEAD 就会处于 detached 状态（游离状态）。

20. 游离分支的弊端
     。弊端就是：在这个基础上的提交会新开一个匿名分支！

20. 解决方法

     方法一：  （当不考虑游离分支就是dev1分支的最新commit时，可用下面的方法解决）
         。在fork上，点击一个分支，如dev1，然后checkout

     方法二：  创建临时分支temp，保存游离分支的修改；再把temp 合并到dev1分支
         。从游离分支的commit处，创建temp分支
         。check out 要用到的dev1分支
         。合并temp分支到dev1  
         。删除temp分支

      具体解决操作：
      git branch -v 查看当前领先多少 
      4449a91 指向的是 dev1 的最后一次提交
      新建一个 temp 分支，把当前提交的代码放到整个分支 
      checkout 出要回到的那个分支，这里是 dev1 
      然后 merge 刚才创建的临时分支，把那些代码拿回来 
      git status 查看下合并结果，有冲突就解决
      合并 OK 后就提交到远端 
      删除刚才创建的临时分支  

      git branch temp
      git checkout dev1
      git merge temp
      git branch -d temp     





--------------------------------------------- 远程分支操作 ------------------------------------------------
 
 1. 查看远程分支
     git branch -r     
     git branch -a    

2. 在本地的master分支推送到远程的master分支

   格式： git  push   远程仓库名  本地分支名 ：  远程分支名

       git push  gitee master:master     //完整写法   
       git push  gitee master               //简略写法      

2. 把本地的devr分支推送到远程的master分支
      git push  gitee dev:master         //完整写法  
      git push  gitee master               //简略写法   

2. 推送到远程分支后，建立关联
    git push -u origin  master    //以后可以直接git push，而不用指定远程和分支

2.强制推送  （谨慎使用， 会删除其他同事的修改）
   git push --force

2. 推送所有分支到远程
   git push --all

2. 向远程推送标签
   git push --tags

3.  删除远端分支   （也可在github网址上删除）
    git push github -d develop       

  。把远端分支下载到本地  （无本地分支）
      方法1：      
      git checkout -b  dev  origin/dev                  //在本地创建一个新的分支，并将远程分支的内容下载到本地分支上 
 
     方法2： 在fork上操作
        右键分支名称  -- checkout  -- track

4.  把远端分支pull到本地 （已有本地分支）

    方法1：
       git checkout  dev
       git pull origin  dev

    方法2：
       git pull origin remote_branch_name:local_branch_name        //这个命令会将远程分支的内容直接合并到本地分支中。如本地分支不存在，则会创建一个新的本地分支

5.  在github设置默认分支： github网址 -- setting  -- 左边点选branch  -- 中间defaut下面 -- 右边 双向箭头  -- 下拉列表选择默认分支



=========================其他=============================


git push origin test:master         // 提交本地test分支作为远程的master分支 //好像只写这一句，远程的github就会自动创建一个test分支
git push origin :test                // 刚提交到远程的test将被删除，但是本地还会保存的，不用担心
删除远程仓库：$ git remote rm [name]
修改远程仓库：$ git remote set-url --push[name][newUrl]
拉取远程仓库：$ git pull [remoteName] [localBranchName]
推送远程仓库：$ git push [remoteName] [localBranchName]
删除远程分支：$ git push origin :heads/[name]


查看版本：$ git tag
创建版本：$ git tag [name]
删除版本：$ git tag -d [name]
查看远程版本：$ git tag -r
创建远程版本(本地版本push到远程)：$ git push origin [name]
删除远程版本：$ git push origin :refs/tags/[name]


添加子模块：$ git submodule add [url] [path]
如：$ git submodule add git://github.com/soberh/ui-libs.git src/main/webapp/ui-libs
初始化子模块：$ git submodule init ----只在首次检出仓库时运行一次就行
更新子模块：$ git submodule update ----每次更新或切换分支后都需要运行一下
删除子模块：（分4步走哦）
1)$ git rm --cached [path]
2) 编辑“.gitmodules”文件，将子模块的相关配置节点删除掉
3) 编辑“.git/config”文件，将子模块的相关配置节点删除掉
4) 手动删除子模块残留的目录


 

