
1. git add时，出现warning: LF will be replaced by CRLF的解决方法，看用下面的方法处理

         git config --global core.autocrlf true

         在进入实操之前，我们先来回顾一下基础知识。行结束符是用于标记文本文件中一行结束的特殊字符。它们在不同的操作系统中有不同的表示：

         LF: \n，在 Unix 和 Unix-like 系统中使用，如 Linux 和 MacOS。
         CRLF: \r\n，在 Windows 系统中使用。

         # 使 Git 在检出文件时将 LF 转换为 CRLF，并在提交时将 CRLF 转换回 LF
         git config --global core.autocrlf true

         # 使 Git 在提交时将 CRLF 转换为 LF，并在检出时不做转换
         git config --global core.autocrlf input

         # 告诉 Git 不要进行任何自动的行结束符转换
         git config --global core.autocrlf false

         # 对于 Windows 用户
         git config --global core.autocrlf true

         # 对于 Unix-like 系统用户
         git config --global core.autocrlf input



2.  从github上clone了一个库下来，如何知道里面的某个目录，是属于哪个分支的

    分析： 现在的库是ommo_nrf， 目录在C:\workspace\ommo_nrf，
             查里面的这个目录： C:\workspace\ommo_nrf\nRF5_SDK_17.1.0_ddde560\examples\ommo\ommo_adc_sampler_generic\13668-analog_adc_siu_3v3-standard

    解决： 右键 -- C:\workspace\ommo_nrf  -- bash
            
          方法1： 只显示说明
           git log --all --oneline -- nRF5_SDK_17.1.0_ddde560/examples/ommo/ommo_adc_sampler_generic/13668-analog_adc_siu_3v3-standard

          方法2：只显示日期
          git log --all --date=short -- nRF5_SDK_17.1.0_ddde560/examples/ommo/ommo_adc_sampler_generic/13668-analog_adc_siu_3v3-standard

          方法3： 显示日期和说明
          git log --all --pretty="%h %ad %s" --date=short -- nRF5_SDK_17.1.0_ddde560/examples/ommo/ommo_adc_sampler_generic/13668-analog_adc_siu_3v3-

                   
      方法3结果： HP@HP-PC MINGW64 /c/workspace/ommo_nrf (develop)
         $ git log --all --pretty="%h %ad %s" --date=short -- nRF5_SDK_17.1.0_ddde560/examples/ommo/ommo_adc_sampler_generic/13668-analog_adc_siu_3v3-standard
         92399efe6 2026-07-09 Added firmware for 14525D, and updated firmware for 14525C.  Moved trace.* to stm32_common so that it can be shared with ST projects; 
                                           trace locations are now defined in ommo_trace_defs.h.
         5ab510306 2026-04-02 Increase stack size to reflect current usage (max 10404 bytes for 12541 standard Release build)




